/// @description Params

// Use for movement calculation
camera = view_camera[0];
following = objPlayer;
viewWidth = camera_get_view_width(camera) * 0.5;
viewHeight = camera_get_view_height(camera) * 0.5;
xDest = xstart;
yDest = ystart;
